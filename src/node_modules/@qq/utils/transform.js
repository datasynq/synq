import os from 'os'

export const create_transform = () => {
	let transform = 'export default function() {\n'
	transform += '\t// ...write your transform code here...\n'
	transform += '\treturn []\n'
	transform += '}' + os.EOL

	return transform
}
